{
  "name": "request-logs",
  "version": "0.0.2",
  "description": "HTTP debug logs for request-compose",
  "keywords": [
    "request",
    "compose",
    "http",
    "debug",
    "log"
  ],
  "license": "Apache-2.0",
  "homepage": "https://github.com/simov/request-logs",
  "author": "Simeon Velichkov <simeonvelichkov@gmail.com> (https://simov.github.io)",
  "repository": {
    "type": "git",
    "url": "https://github.com/simov/request-logs.git"
  },
  "dependencies": {
    "chalk": "^1.1.1",
    "prettyjson": "^1.1.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "bin": {},
  "main": "./index.js",
  "files": [
    "build/",
    "index.js",
    "LICENSE",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build:logs": "npx babel logs.js --out-file build/logs.js",
    "build": "rm -rf build/ && mkdir -p build && npm run build:logs",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
