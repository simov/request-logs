{
  "name": "request-logs",
  "version": "1.0.0",
  "description": "HTTP debug logs for request-compose",
  "keywords": [
    "request",
    "compose",
    "http",
    "debug",
    "log"
  ],
  "license": "Apache-2.0",
  "homepage": "https://github.com/simov/request-logs",
  "author": "Simeon Velichkov <simeonvelichkov@gmail.com> (https://simov.github.io)",
  "repository": {
    "type": "git",
    "url": "https://github.com/simov/request-logs.git"
  },
  "dependencies": {
    "colors": "^1.4.0",
    "prettyjson": "^1.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  },
  "bin": {},
  "main": "./build/index.js",
  "files": [
    "build/",
    "index.js",
    "logs.js",
    "LICENSE",
    "README.md",
    "package.json"
  ],
  "scripts": {
    "build:main": "babel *.js --out-dir build",
    "build": "rm -rf build/ && mkdir -p build && npm run build:main",
    "prepublish": "npm run build"
  },
  "engines": {
    "node": ">=4.0.0"
  }
}
